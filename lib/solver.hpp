/*
  Robbie Alexander 
 
 */ 

template<typename T, int Dims> 
class Solver{
  
};

template<typename T, int Dims>
class EllipticSolver : Solver<T, Dims> {

};

template<typename T, int Dims>
class HyperbolicSolver: Solver<T, Dims> {

};
